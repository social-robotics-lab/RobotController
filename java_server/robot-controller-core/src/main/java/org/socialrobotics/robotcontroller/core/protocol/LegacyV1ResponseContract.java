package org.socialrobotics.robotcontroller.core.protocol;

/** Describes whether a successful legacy v1 command writes a response frame. */
public enum LegacyV1ResponseContract {
    NO_RESPONSE,
    RESPONSE_FRAME
}

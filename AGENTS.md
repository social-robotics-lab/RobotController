# AGENTS.md
## 1. この文書の目的

このファイルは、Codexを含む自動コーディングエージェントが本リポジトリを変更する際に、常に従うべき開発原則を定める。

本プロジェクトの目的は、既存のJava版RobotControllerと正常系で通信互換性を持ち、Windows上のモック環境と実機ロボット上の双方で動作するPython版RobotControllerを開発することである。

## 2. 参照すべき情報源

判断に迷った場合は、次の優先順位で参照すること。

1. 本リポジトリの `AGENTS.md`
2. `docs/requirements.md`
3. `docs/protocol-compatibility.md`
4. `docs/architecture.md`
5. `docs/migration-plan.md`
6. 既存のJava版RobotController実装
7. `RobotController_TCP_Server_Documentation_ja.md`
8. `social-robotics-lab/robo-tutorial` のクライアント実装
9. 非公式の低レベル実装や外部資料

文書と実装に不一致がある場合は、独断で仕様を変更せず、不一致を報告すること。

## 3. 変更範囲

* Python版のコードは原則として `python_server/` 以下に配置する。
* 既存Javaコードは、明示的な指示がない限り変更しない。
* 既存Javaコードは、通信互換性と機能比較の参照実装として扱う。
* 既存Java版の不具合、例外、競合、ハングを意図的に再現しない。
* 正常な既存クライアントとの通信互換性を維持する。
* 新機能や互換性を破る変更は、レガシープロトコルとは分離して設計する。

## 4. 実装方針

### 4.1 ハードウェア抽象化

* TCPサーバー、プロトコル処理、モーション制御と、実機デバイスへのアクセスを分離する。
* すべてのロボット制御は共通のバックエンドインタフェースを介して行う。
* Sota、CommU、Dogは個別のバックエンドまたはロボットプロファイルとして実装する。
* 将来のロボット追加時に、TCPサーバーやコマンド処理を変更せず追加できる構造にする。
* Windows上の開発とテストでは、必ずモックバックエンドを使用する。

### 4.2 通信互換性

レガシープロトコルv1について、次を維持する。

* 4バイトのbig-endian長さヘッダ
* 正常系で有効な長さは `0 <= length <= min(command-specific configured limit, 2147483647)`
* `0x80000000` 以上の値は、既存Java版では負数になるため拒否する
* Frame Codecは長さ0を `b""` として処理し、必要な拒否はコマンド層で行う
* Frame Codecは固定最大値を内部に持たず、呼び出し側から `max_length` を受け取る
* ヘッダに続く指定バイト数のデータ本体
* コマンド名を第1フレームとして送信
* 必要なコマンドではペイロードを第2フレームとして送信
* 原則として1接続1コマンド
* `read_axes`のみ正常時に応答フレームを返す
* 既存クライアントが応答を読まないコマンドには、v1ではACKを追加しない

構造化された成功・エラー応答は、後方互換なプロトコルv2として別途設計する。

本プロジェクトで通信互換性があるとは、正常な既存クライアントが送るバイト列を受理し、同じコマンド効果を発生させ、v1では `read_axes` だけが応答を返すことをいう。処理タイミングの完全一致、JSONキー順序、JSON空白、不正入力時のJava固有挙動、Java版のクラッシュ・ハング・競合およびJavaの暗黙的な数値変換は互換性の対象外とする。

### 4.3 入力検証

以下を必ず検証する。

* フレーム長
* コマンド名の長さと文字コード
* JSONの構文とトップレベル型
* 必須フィールド
* 数値の型と有限性
* サーボ名とLED名
* サーボ角度とLED値の範囲
* `Msec`、`Pause`、`Speed`の範囲
* モーション要素数
* WAVデータのサイズと最低限の形式
* キュー長と接続数

不正入力を暗黙変換、切り捨て、または推測によって受理しない。

### 4.4 同時実行制御

* Pose、Motion、Idle Motionが同時にハードウェアへ指令を送らないようにする。
* 初期版のTCP接続処理は、同期ソケットと上限付き `ThreadPoolExecutor` を使用する。
* 初期版では `asyncio` を採用しない。
* ハードウェア操作をネットワークスレッドから直接行わない。
* ハードウェアアクセスは別の単一ワーカーへ渡して直列化する。
* 停止コマンドは冪等にする。
* 開始前や停止済みの機能に停止コマンドを送っても例外を発生させない。
* Pose、Motion、Idle Motionには世代IDまたはキャンセルトークンを割り当てる。
* 停止または置換された古い世代は、その後ハードウェア指令を送信できない設計にする。
* 音声再生プロセスは、サーバー自身が起動したプロセスだけを停止する。
* 固定名の一時ファイルを使用しない。

### 4.5 起動状態

起動状態名は `starting`、`initializing`、`ready`、`degraded`、`stopping`、`stopped`、`failed` に統一する。

初期版は、バックエンドの初期化が成功して `ready` へ遷移してからTCP listenを開始する。`ready` 前にロボットコマンドを実行できる経路を作らない。

## 5. 安全に関する禁止事項

Codexを含む自動エージェントは、許可の有無にかかわらず、実機デバイス、`systemctl`、サーボ、LEDおよびトルクを操作してはならない。以下は絶対的な禁止事項である。

* `/dev/i2c-1`への書き込み
* `/dev/ttyMFD1`への書き込み
* その他の実機デバイスファイルへの書き込み
* `systemctl stop vsmd_edison`
* `systemctl disable vsmd_edison`
* サーボのON・OFF
* サーボ位置指令
* LED点灯指令
* `killall`や無差別なプロセス終了
* 実機上の設定ファイルの変更
* 実機への自動デプロイ
* 実機上での自動テスト

実機用コードを作成する場合もエージェントは実行しない。エージェントが手順を作成し、人間が内容を確認して手動実行すること。

## 6. サーボ安全要件

* 未確認のサーボID、変換比、初期位置、可動範囲を推測しない。
* ロボット種別ごとに検証済みのサーボプロファイルを使用する。
* 実機試験では、最初に角度読み出しを確認する。
* 動作試験は1軸、小さい変位、低速、短時間から開始する。
* 範囲外値を丸めるか拒否するかは文書化された仕様に従う。
* 不明な場合は拒否し、ログへ記録する。
* プロセス異常終了時に無条件でトルクを切らない。機体の転倒や落下を考慮する。

## 7. コード品質

* Pythonコードには型ヒントを付ける。
* 本番コードはすべてCPython 3.6.15で構文解析・実行できるようにする。
* インタフェースには原則として `abc.ABC` と `abc.abstractmethod` を使用する。
* 型注釈には `typing.List`、`typing.Dict`、`typing.Mapping`、`typing.Optional` 等を使用する。
* `from __future__ import annotations`、組み込み型ジェネリクス、`X | None`、標準ライブラリの `dataclasses`、`typing.Protocol` および `asyncio.run()` を使用しない。
* Python 3.7以降で追加された構文、f-string構文および `pathlib`、`typing`、`asyncio` 等の標準ライブラリAPIを使用しない。
* 公開クラス、公開関数、複雑な処理にはdocstringを付ける。
* プロトコル処理にハードウェア固有コードを含めない。
* OS依存処理は専用モジュールへ隔離する。
* グローバルな可変状態を避ける。
* 終了処理はコンテキストマネージャーまたは明示的な `close()` を用いる。
* `__del__()`だけにリソース解放を依存しない。
* 例外を握りつぶさず、適切な型の例外へ変換してログを残す。
* 時刻計測には、経過時間であれば単調増加時計を使用する。
* テストで実時間の長いsleepを使用しない。時計やスケジューラを注入可能にする。

## 8. 依存関係

* 実機Edison上の本番実行環境はCPython 3.6.15とする。
* 開発ツール自体は新しいPython上で動作してよいが、本番コードをPython 3.6非互換の構文へ自動変換してはならない。
* 新しい依存ライブラリを追加する場合は、次を確認する。

  * 対応Pythonバージョン
  * Yocto OSでの導入可能性
  * ネイティブ拡張の有無
  * ライセンス
  * 依存を使わず標準ライブラリで実現できないか
* ハードウェア層は可能な限り標準ライブラリで実装する。
* 開発専用依存と実機実行依存を分離する。

非公式な低レベル実装を参照するときは、リポジトリURL、検証済みの完全なcommit SHA、確認日、ライセンス、コピーか仕様のみを参考にした再実装か、および各レジスタ・ID・パケット・可動範囲の検証状態を記録する。未確認の値を推測せず、完全なcommit SHAを確認できない場合はTODOとして残す。外部リポジトリの具体的なSHAは、本ファイルではなく `docs/protocol-compatibility.md` のReference baselines節へ記録する。

## 9. テスト

変更には、原則として対応するテストを追加する。

最低限、次をテストする。

* フレームのエンコードとデコード
* 部分受信
* EOF
* タイムアウト
* 長さ0
* 過大フレーム
* 不正UTF-8
* 不正JSON
* 未知のコマンド
* 正常なPose、Motion、Idle Motion
* 不正な数値
* キャンセルと停止
* 複数コマンドの競合
* `read_axes`のJSON応答
* WAV再生の開始、置換、停止
* モックバックエンドへの正しい呼び出し

既存の `robo-tutorial` クライアントと同じバイト列を用いる互換性テストを含める。

pytestはPython 3.6対応版を固定して使用する。初期候補は `pytest==6.2.5` とし、変更する場合はCPython 3.6.15対応を確認して文書化する。

実機テストは自動テストと分離し、明示的なオプトインがない限り実行されないようにする。

## 10. タスクの進め方

大規模な一括実装を避け、検証可能な小さい変更に分割する。

各タスクでは、次の順序を守る。

1. 関連文書と既存実装を確認する
2. 変更範囲と受入条件を明示する
3. 必要なテストを先に追加または定義する
4. 最小限の実装を行う
5. テスト、型検査、静的解析を実行する
6. 差分を確認する
7. 未解決事項を報告する

## 11. 作業完了時の報告

作業完了時には、次を簡潔に報告すること。

* 変更したファイル
* 実装した内容
* 実行したテストと結果
* 実行しなかったテスト
* 互換性への影響
* 安全性への影響
* 残っている仮定または未解決事項
* 実機試験が必要な場合の手順

テストに合格していない場合や、仕様が不明な場合は、完了したと表現しない。
